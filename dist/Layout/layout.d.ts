import { window } from "../entities/form/window";
export declare class LayoutFrame {
    P: string;
    constructor(P: string);
    configureLayout(window: window, principalElementRucula: HTMLElement): void;
}
